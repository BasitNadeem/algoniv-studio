# Contact form delivery (https://web3forms.com — free, no account server-side).
# Without this the contact form falls back to a mailto: link.
# Set the same value as an Environment Variable in the Vercel project.
VITE_WEB3FORMS_KEY=
